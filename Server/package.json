{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Mock server for storing appointment data",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
